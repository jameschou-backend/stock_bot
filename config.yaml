FINMIND_TOKEN: ""
DB_DIALECT: "mysql"
DB_HOST: "127.0.0.1"
DB_PORT: 3307
DB_NAME: "stock_bot"
DB_USER: ""
DB_PASSWORD: ""
TOPN: 10
LABEL_HORIZON_DAYS: 20
TRAIN_LOOKBACK_YEARS: 5
MIN_AVG_VOLUME: 200000
ROUND_TRIP_COST: 0.00585
HOLD_BUFFER: 10
BACKTEST_FOLDS: 6
BACKTEST_VAL_MONTHS: 3
SCHEDULE_TIME: "16:50"
TZ: "Asia/Taipei"
API_HOST: "0.0.0.0"
API_PORT: 8000
DASHBOARD_SHOW_ML: false
BOOTSTRAP_DAYS: 365
MIN_AVG_TURNOVER: 0.5  # 20日平均成交值門檻（億元），0.5 = 5000萬，排除冷門股
MIN_AMT_20: 50000000  # 20日平均成交值門檻（元），與 MIN_AVG_TURNOVER 並存，優先使用此欄位
USE_ADJUSTED_PRICE: true  # true: ret/ma/breakout 使用還原價；false: 使用原始 close
ENABLE_TRADABILITY_FILTER: true  # true: 啟用狀態硬過濾（halt/suspend/disposition...）
FALLBACK_DAYS: 10
# 大盤過濾器（市場下行時減碼）
MARKET_FILTER_ENABLED: true
MARKET_FILTER_MA_DAYS: 60        # 用 60 日均線判斷市場多空
MARKET_FILTER_BEAR_TOPN: 5       # 空頭市場時只選 5 檔
MARKET_FILTER_WEEKLY_DROP_THRESHOLD: -0.03  # 週跌幅 > 3% 時進入危機模式
MARKET_FILTER_CRISIS_TOPN: 3     # 危機模式只選 3 檔
REGIME_DETECTOR: "ma"            # 市場狀態偵測器（目前支援 ma）
# 季節性過濾器（歷史統計弱勢月份降倉）
SEASONAL_WEAK_MONTHS: [3, 10]    # 3月、10月系統性弱勢
SEASONAL_TOPN_MULTIPLIER: 0.5    # 弱勢月份 topN 乘以此係數（0.5 = 減半）
# 停損冷卻機制
STOPLOSS_COOLDOWN_WEEKS: 3       # 停損觸發後幾週內不再選入同支股票
DATA_QUALITY_MODE: "research"    # strict|research|dev
SELECTION_MODE: "model"          # model|multi_agent
MULTI_AGENT_TOPN: 20
MULTI_AGENT_WEIGHTS: {"tech": 0.35, "flow": 0.30, "margin": 0.10, "fund": 0.15, "theme": 0.10}
# 回測基礎設定
STOPLOSS_PCT: -0.07              # 停損比例 (-7%)
TRANSACTION_COST_PCT: 0.001425   # 單邊手續費 0.1425%（每筆最低 20 元由回測引擎處理）
# 回測進階設定
BACKTEST_ENTRY_DELAY_DAYS: 1     # 進場延遲交易日（1=決策日次一交易日執行，更符合實際）
BACKTEST_RISK_FREE_RATE: 0.015   # Sharpe 計算用無風險利率（年化，台灣定存約 1.5%）
BACKTEST_BENCHMARK_WITH_COST: true  # Benchmark 是否套用相同交易成本（公平比較）
BACKTEST_POSITION_SIZING: "equal"   # equal | score_tiered | vol_inverse
BACKTEST_TRAILING_STOP_PCT: null    # 移動停利比例（如 -0.12），null 停用
BACKTEST_ATR_STOPLOSS_MULTIPLIER: null  # ATR 倍數動態停損（如 2.5），null 使用固定停損
BACKTEST_ATR_PERIOD: 14          # ATR 計算週期（日）
# 過熱過濾器（選股階段排除短線過熱標的）
ENABLE_OVERHEAT_FILTER: false    # 是否啟用過熱過濾（建議先在回測中評估效果再開啟）
OVERHEAT_RSI_THRESHOLD: 75       # RSI 過熱門檻（RSI > 此值視為短線過熱）
# 突破確認進場（F+ 策略整合）
ENABLE_BREAKTHROUGH_ENTRY: true  # 選股後標記突破狀態，輸出可進場/等待中/候補三清單
STRATEGY_WEIGHTS_BULL: {"MomentumTrend": 0.6, "MeanReversion": 0.2, "DefensiveLowVol": 0.2}
STRATEGY_WEIGHTS_BEAR: {"MomentumTrend": 0.2, "MeanReversion": 0.3, "DefensiveLowVol": 0.5}
EVAL_TOPK_LIST: [10, 20]         # ranker 驗證指標使用的 K 值
# API Rate Limiting
FINMIND_REQUESTS_PER_HOUR: 6000  # FinMind API 每小時請求限制（付費會員 6000，免費 600）
CHUNK_DAYS: 180  # 資料抓取每個 chunk 的天數（建議 180 天減少 API 次數）
INSTITUTIONAL_BULK_CHUNK_DAYS: 90  # 法人全市場抓取建議 chunk 天數（約 3 個月）
MARGIN_BULK_CHUNK_DAYS: 90  # 融資融券全市場抓取建議 chunk 天數（約 3 個月）
FINMIND_RETRY_MAX: 3  # FinMind API 最大重試次數
FINMIND_RETRY_BACKOFF: 1.0  # FinMind API 重試退避秒數（指數退避）
