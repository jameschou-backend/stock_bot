# 三帳本結算前清單：2026-09-14

這次補的是當日結算準備與可核對的帳列現金。策略、原排序對照、0050 基準各自保留帳本；沒有新增成交、人工核准、股息、淨值或隔日訊號，也未修改封存策略及逐筆來源守門程式。

## 操作位置

工作台「策略驗證」→「實戰前準備：成交金額排序的每日模擬」→選擇帳本→「今晚能結算嗎？先看缺什麼」。

- 列出交易日曆、18:00 結算時段、未成交委託、當日行情、公司行動來源、人工核對及現金差額。
- 顯示當日每筆委託的買賣、整股／零股、成交與剩餘股數、取消狀態和費稅。
- 「下載三帳本結算前清單」提供讀取時間、帳本末筆雜湊及來源雜湊；這是當時快照。
- 有持股時，使用下方原有「有持股後：顯示每日公司行動核對」，由核對人逐帳本保存。清單本身不會核准。
- 正式結算仍由既有排程重跑完整檢查；前置項目通過不代表券商成交已驗證。

## 本次實際快照

讀取時間為台北 2026-09-14 11:44:30，檔案 `.cache/capacity-close-preview-20260914-review.json`。

| 帳本 | 模擬持股 | 今日買進金額（未含費） | 今日費稅 | 帳列現金 | 現金收支差額 |
|---|---|---:|---:|---:|---:|
| 成交金額排序 | 1560 中砂 4 股 | 3,044 | 20 | 996,936 | 0 |
| 原排序現金對照 | 1560 中砂 4 股 | 3,044 | 20 | 996,936 | 0 |
| 0050 獨立基準 | 0050 5,271 股 | 565,574.10 | 806 | 433,619.90 | 0 |

三帳本當時都仍有一筆未完全成交委託，尚無今日人工核對。DB 行情日期為 9/11，尚無 9/14 收盤持股價格。因此全部顯示等待，並未以成本或前一日淨值填補今日資產。

現金等式：當日期初帳列現金－當日買進總價＋當日賣出總價－費稅＋已交付現金股息。這是帳列現金核對，不是銀行交割款核對；未交付權益不計入可用現金。

## 已知公司行動與證據限制

- 0050：交易所列示 2026/7/21 除息、8/10 發放、每單位 0.6 元。[TWSE ETF 配息資料](https://www.twse.com.tw/en/ETFortune-institute/dividendList?startDate=&stkNo=0050)。這早於本帳本 9/14 首次買進，不能計入新持股收益。
- 1560：本地 FinMind 快取列示 2026/7/16 除息、8/6 發放、現金合計 4.94205668 元；同樣早於首次買進。本次尚未取得可直接對照最終條款的公司官方公告全文，故不標示為完整官方核驗。
- 空白事件列表與來源尚在有效期，只表示沒有發現已知衝突；不證明當日公告、停復牌及公司行動全部涵蓋。人工核對仍保留。

## 實作與效能

`app/capacity_close_preview.py`、`app/capacity_close_preview_ui.py` 及 `scripts/preview_capacity_close.py`。

讀取與排程共用 `.run.lock`，忙碌時立即提示；保留三帳本、父版本及來源守門的指紋驗證。帳本事件以讀取時點截斷，不能把稍後成交當成當時已知；行情查詢只取當日持股。公司行動全部讀本機快取，清單本身新增 FinMind request 為 0。來源過期、日曆缺失、持股價格缺失時均明示缺件，不以預設價格或平日推測補齊。

唯讀輸出命令（使用新的檔名；已有檔案拒絕覆寫）：

```sh
python scripts/preview_capacity_close.py --output .cache/capacity-close-preview-new.json
```

## 驗證

針對性測試 7 項通過：盤中不能結算、未來成交不納入舊時點、讀取不改帳本、不抓外部來源、晚間仍須取消及人工核對、缺失／非正數行情、來源過期、缺少日曆、排程鎖競爭、賣出與股息交付現金核對，以及兩項工作台回歸。

完整驗收：

- `make test`：2,529 passed、28 warnings、66.16 秒。第一次完整測試發現一般記帳 UI 案例意外讀取本機前向帳本；已將其與具有獨立測試的 capacity UI 隔離，重跑全部通過。日誌 `.cache/close-preview-make-test-final.log`。
- `make pipeline`：成功。使用 FinMind 價格來源、停用額外 Sponsor ingest，並先核對既有 TLS bundle SHA256；未修改 `.env`。日誌 `.cache/close-preview-pipeline.log`。
- 既有 `make api` 服務：curl `/health` 回傳 `status=ok`，`/picks`、`/models`、`/jobs?limit=10` 均成功且為 JSON 列表。
- 工作台已重新載入程式；台北 11:54:27 在實際瀏覽器「策略驗證」頁確認清單、20 元費稅、0 元現金差額、人工核對提示與下載按鈕可見。沒有代勾或保存人工核對。

## 尚未完成

今天仍須在實際時段完成取消餘單、更新收盤資料與人工核對，再由排程結算。本次不補造未觀察期間的成交，也不將歷史回測報酬移入模擬帳本。完整跨日逐筆回測仍受歷史零股成交證據缺漏限制；實盤資格仍為 false。
