# 單組連續帳戶回測驗收

2026-09-27 完成，操作說明見 [單一連續帳戶回測](index_case_backtest.md)。

以已封存的 `equal_7` 驗收：本金100萬元、2016-01-04至2026-09-09、2,604個交易日，中間不重設資金。固定75%正二配置，其餘現金；滑價加倍、買進與賣出各多延遲一個交易日。這不是新增參數搜尋，也未更換原始研究主規則。

| 操作 | 重新執行 | 重用 | 核對來源 | 全程耗時 |
| --- | ---: | ---: | ---: | ---: |
| CLI首次 | 1 | 0 | 11.719秒 | 44.449秒 |
| CLI再次 | 0 | 1 | 12.489秒 | 12.515秒 |
| 工作台按鈕 | 0 | 1 | 12.409秒 | 12.435秒 |
| 工作台強制重算 | 1 | 0 | 11.913秒 | 44.422秒 |

四次均離線，網路請求0次、資料庫寫入0次。背景程序另有啟動及完成檔驗證時間；例如強制重算工作紀錄全程46秒。兩次新計算登記試驗，兩次快取重用不增加試驗次數。

每次完整核對帳戶、每日決策、原始委託、未完成委託、獨立帳務稽核及摘要。結果與封存帳戶一致：期末19,921,620元、累積淨報酬1892.162%；同成本0050為873.6571%。完整帳戶下載已在工作台實際操作成功。

已測試來源在執行前或途中改動、現金／資產篡改、來源身分不符、快取收據損壞、工作情境不符、強制重算卻只回傳快取、違規網路連線、覆寫既有帳戶、程序成功退出但結果不完整等情境；均拒絕作為成功證據。背景工作沿用一次一個的限制，不建立排程。

完整驗收：`make test` 3731項通過、30項既有警告，80.84秒；`make pipeline` 使用既有來源暫停保護完成；`make api` 後以curl驗證health、picks、models、jobs四個端點均200。pipeline仍記錄TWSE來源暫停，這不代表相關資料已修復。

本機原始驗收位於 `.cache/index-case-backtest/acceptance/`；背景工作識別碼為 `423279dd130a485d871ccc1080d788cb`（重用）及 `2dd0c87a966e43f4a0383cf496af4e46`（強制重算）。

資格不變：`live_qualified=false`、`strict_data_ready=false`、`unseen_validation=false`。每日限價含規則推算、0050歷史配息支付日尚未全部獨立核對，日資料不能證明真實成交。槓桿ETF的歷史優勢也不是個股選擇能力或未來獲利保證。所有已研究期間仍列為看過的資料。
