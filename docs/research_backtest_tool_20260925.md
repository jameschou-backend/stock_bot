# 2026-09-25 回測工具驗收紀錄

本次優先整理回測工具，沒有新增選股策略、變更舊帳戶報酬、重啟排程或送出券商委託。

## 交付範圍

統一 CLI `scripts/run_verified_backtest.py` 與工作台「策略驗證 → 固定條件回測工具」。固定目前已核對的 5 檔現金策略、本金 100 萬、2022-01-03 至 2026-09-09、458 筆候選訊號；選擇整張／含零股、一般／加嚴情境，0050 使用同條件配對。

入口提供來源預檢、背景回測、強制重算、逐案中斷續跑、每日資產曲線、全部買賣與每日資產 CSV、JSON 報告。逐筆模式只檢查證據是否足夠，缺完整撮合序列時中止，不提供替代的日資料績效。

原封存研究保留；新案例依資料、程式、環境與設定識別保存在獨立目錄。沒有更改舊的交易引擎。每次開始及發佈前核對來源內容，不允許報告寫入封存資料目錄。

## 實際重算及重用

正式驗收經工作台 submit → worker → CLI → 檔案驗證執行：

| 執行 | 工作識別 | 重算／重用 | 耗時 |
| --- | --- | --- | --- |
| 強制重算 | 737d3807a2544365bd67c9c9de74cb3e | 8／0 案例 | 39.333 秒 |
| 相同輸入重跑 | 410a03cefbd6443cae387c3e0a1b7639 | 0／8 案例 | 19.758 秒 |

來源共 24,117 個檔案。兩次輸入識別相同，案例內容與配對比較相同；全部使用 0 次 FinMind、0 次網路請求、0 次 DB 寫入。重跑包含兩次實際內容雜湊核對及帳務再稽核，不是只回傳上次數字。以上是本機兩次實測，不保證其他硬體或負載下相同秒數。

原始證據位於 `.cache/workbench/jobs/<工作識別>.result.json`、同名工作紀錄與 log；比較驗收為 `.cache/backtest-tool/acceptance.json`。較早開發試跑另留在 `.cache/backtest-tool`，不能與最終驗收混用。

## 結果界線

八案中六案完成日資料模型，兩案因公司行動缺件中止：

- 整張策略一般情境：2881，2025-09-25，缺核實配股條件。
- 整張策略加嚴情境：2880，2025-08-13，缺核實配股條件。

UI 對這兩案不顯示全期報酬。普通盤和零股日量不是盤中送單後的成交證據；完整歷史母體、公告與修訂版本、未見期間也尚未全部驗證。工具完成不等於策略已取得實盤資格。

## 回歸檢查

新增檢查覆蓋：未來日期、跳過下一交易日、缺流動性歷史、缺交易日、同日買入訊號、比較本金／費用不同、現金帳不平、結果被改寫、收據與設定不一致、只寫完半份結果、中斷後續跑、重新計算與快取一致、CSV 欄位順序、來源在 warm run／預檢中途變動、輸出污染封存資料、UI 顯示數字與附檔不一致。

實作過程先由測試抓出 CSV 首次／重跑欄位排序不一致，已改用固定排序；獨立審查指出 warm run 的結束來源檢查及報告路徑範圍，兩項都已補測並修正。

完整驗收：`make test` 2,870 passed、29 warnings、73.81 秒；`make pipeline` 連續兩次成功，兩次 features／labels 增量均為 0。重新啟動 `make api` 後，health、picks、models、jobs 四個 curl 均 HTTP 200。實際瀏覽器確認新入口、八案重用狀態、缺件說明、每日資產圖及明細下載皆顯示。

Pipeline 沿用既有研究資料品質模式，仍記錄最近行情的大幅波動警告；通過本輪工具回歸不代表即時行情已全面認證。測試、pipeline、API 證據保留於 `.cache/backtest-tool-make-test.log`、`.cache/backtest-tool-pipeline-{1,2}.log`、`.cache/backtest-tool-{health,picks,models,jobs,jobs-second}.json`。最後再執行逐筆預檢，確認 pipeline 後封存來源仍通過核對，逐筆缺件仍正常阻擋。
