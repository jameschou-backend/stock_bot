# 前向驗證依序改善：2026-09-13

現金版選股、配置、會計與撮合核心不變。新增旁路檢查面板和CLI，讀取封存帳本、公司行動來源與執行日誌，不代替人工核對、不建立成交。八份原始與模擬帳本最後雜湊均與9/12驗收一致。

## 1. 公司行動核對

先更新策略1560與基準0050來源：實際查詢9次，重用1筆，沒有已知衝突；稍後使用新功能再查詢，0次新呼叫、重用10筆。這是當次資料新鮮度，不代表明日仍有效或官方公告完整。

工作台「策略驗證」新增「依序完成：資料核對 → 每日流程 → 成交比對」。一個按鈕更新兩份帳本，合計每批最多12次查詢，與現金排程共用執行鎖，沿用FinMind快取與限流；開頁不抓取資料。每日核對依來源指紋與日期判定有效性，過期、來源改變或有衝突均保持待辦。

目前沒有持股，不應誤稱人工確認已完成。有成交持股後，依原封存規則仍須由使用者核對當日公告並保存確認，才可結算。

## 2. 完整交易日流程

新增隔離帳本整合測試，使用真正的現金排程、撮合與會計程式；行情、訊號、pipeline與收盤資料邊界使用測試資料。驗證兩次快照形成200股部分成交、餘799股取消、重複快照不重複成交、未核對不能收盤、測試人工確認後續跑、pipeline不重跑、結算後不新增0050，以及原帳本不變。

這是合成資料的程式演練，沒有模擬真實交易日已經發生。正式帳本仍0筆成交、0天完整流程。9/14仍為第一個預定觀察交易日。面板依兩份帳本盤中處理、取消、pipeline、訊號、結算、隔日計畫九個階段統計完成日；無委託日可正常略過行情，但不產生成交證據。

## 3. 成交差異

新增每筆計畫的成交比例、未成交股數、加權均價、費稅與相對限價價差，附在可下載JSON中。無成交的均價與價差是未知，不補成0。兩份模擬帳本分開，取消剩餘不算成交。

真實帳本API目前回報`initialized=false`，沒有可供驗證的券商成交。相對限價價差僅作診斷，不等於真實市場滑價；還需要有時間、股數、價格與稅費的獨立成交憑證才能比較排隊與執行落差。此輪沒有建立真實帳戶、下單或假造憑證。

## 4. 策略有效性與歷史重現

重新執行`python scripts/research_cash_allocation.py --verify`，在父來源檢查失敗：`Frozen source hash changed: app/finmind.py`。保留舊程式封存與報酬資格限制，沒有更新雜湊掩蓋差異，也沒有把舊績效移植到新版本。

已逐檔核對4,154份封存來源，只有`app/finmind.py`不同；找到相符舊版本`ead1b10ee3432d66f36e4515aef956a10dd0fc64`，將來源複製至隔離目錄，沒有改動工作專案或複製`.env`。原嚴格驗證在隔離位置因絕對快取路徑不同而失敗，因此另做明確標記的搬移診斷：僅將`cache_directory`與父／子`child_cache_directory`的比較值正規化到原位置，檔案內容雜湊仍完整檢查，所有網路請求直接拒絕。這不是未修改的原驗證器直接通過，也不會自動提升UI資格。

在該隔離診斷環境進一步離線重播：benchmark、always_0050、cash、trend_0050四個帳戶的完整狀態、決策、成交和資產結果均與舊封存逐字編碼一致，耗時10.017秒、0次API。詳見[檔案盤點](../artifacts/forward_simulation/historical_inventory_20260913.json)、[隔離檢查與重播結果](../artifacts/forward_simulation/isolated_historical_audit_20260913.json)及[隔離診斷程式](../artifacts/forward_simulation/isolated_historical_audit_20260913.py.txt)。

這解決了本輪舊結果能否重現的疑問；還原價對帳、資料實際公布時間與歷史股票名冊仍未完整解決。新現金策略的未見區間績效只能隨後續交易日累積，本次未新增報酬結論。

## 5. 運作可靠性與驗收

API與工作台原先未運作，已透過`make api`、`make workbench`重新啟動。四個API以curl驗證200且可解析。既有現金版排程仍啟用，明確使用FinMind，沒有改動排程或環境機密。

預設官方行情`make pipeline`仍遇TPEx SSL連線錯誤；`INGEST_PRICES_SOURCE=finmind make pipeline`完整成功。不關閉TLS、不自動換來源，官方端問題仍未解決。本機與MySQL需可用，持續運轉及交易日喚醒尚待實測。

完整測試2333 passed、28 warnings、68.16秒；另新增原始帳本展開回歸測試1 passed。瀏覽器已確認待辦面板、零成交與現金金額；並修正舊版巢狀展開面板造成頁面報錯的問題，改為勾選才顯示原始帳本。

只查狀態：`python scripts/check_forward_readiness.py`。有界更新：`python scripts/check_forward_readiness.py --refresh`。兩者皆不批准交易、不保存人工確認；報告可見具體下一步。

[驗收、來源狀態、帳本雜湊與待辦快照](../artifacts/forward_simulation/readiness_20260913.json)。
